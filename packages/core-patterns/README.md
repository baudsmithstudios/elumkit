# core-patterns

Semantic HTML snippets for the v0.1 component set.

These patterns are designed for plain HTML first and can be copied into Eleventy templates directly.

- `snippets/index.html` includes examples for all current MVP components.
