# core-patterns

Semantic HTML snippets for the component set.

These patterns are designed for plain HTML first. Copy them into your app, commit them, and modify them directly.

- `snippets/index.html` includes examples for all the current components in the library.
